defaults:
  - datamodule: interpolation
  - model: interpolation_tester
  - trainer: default

experiment_name: interpolation-test
project_name: grind-neuralpde
logging_suffix: ""

train_from_checkpoint: last
test_from_checkpoint: 

train: False
test: True

hydra:
  run:
    dir: output/hydra/${experiment_name}/${now:%Y-%m-%d_%H-%M-%S}